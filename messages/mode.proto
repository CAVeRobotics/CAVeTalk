syntax = "proto3";

package cavetalk;

enum Mode {
    MODE_DISARMED = 0;
    MODE_ARMED_MANUAL = 1;
    MODE_ARMED_AUTO = 2;
}

message SetMode {
    Mode mode = 1;
}

message GetMode {
    Mode mode = 1;
}
