edition = "2023";

package cavetalk;

enum Fault {
    option features.enum_type = CLOSED;

    FAULT_MEMORY = 1;
    FAULT_SCHEDULER = 2;
    FAULT_TIMER = 4;
    FAULT_MOTOR = 8;
    FAULT_SERVO = 16;
    FAULT_ACCELEROMETER = 32;
    FAULT_GYROSCOPE = 64;
    FAULT_ENCODER = 128;
    FAULT_COMMS = 256;
    FAULT_LOGGING = 512;
    FAULT_RGBW = 1024;
    FAULT_BUZZER = 2048;
}

message Faults {
    uint32 mask = 1;
}

message ClearFaults {
    uint32 mask = 1;
}
