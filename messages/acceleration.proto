syntax = "proto3";

package cavetalk;

message Acceleration {
    optional float x_meters_per_second_squared = 1;
    optional float y_meters_per_second_squared = 2;
    optional float z_meters_per_second_squared = 3;
}
